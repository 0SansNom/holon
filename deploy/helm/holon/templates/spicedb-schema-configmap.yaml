# Mount SpiceDB schema file for service startup and authorization seeding.
apiVersion: v1
kind: ConfigMap
metadata:
  name: holon-spicedb-schema
data:
  schema.zed: |
{{ .Files.Get "files/spicedb-schema.zed" | indent 4 }}
